language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bourbon-neat' ]; then cd .. && eval "mv $currentfolder generator-bourbon-neat" && cd generator-bourbon-neat; fi

